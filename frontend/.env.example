# Build-time settings (used by `npm run dev` / `npm run build`).
# In Docker these are set at runtime instead - see the root .env.example.
VITE_API_URL=http://localhost:3000/api/v1
VITE_APP_NAME=Your Company Portal
VITE_APP_TAGLINE=Workforce & Task Management
VITE_DEMO_MODE=false
